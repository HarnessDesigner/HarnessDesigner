# © 2025-2026 Kevin G. Schlosser <kevin.g.schlosser@gmail.com>

"""3D-editor add-placement handlers -- one module per object type that
can be placed in this view (see ``objects.objects_3d.<type>.<Type>.
start_add`` for each type's own classmethod entry point). Every object
type supports placement here (unlike the schematic/pegboard views,
which only support a subset -- see each of those packages' own
docstring for which).
"""
